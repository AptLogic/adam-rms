## 2026-02-22 - Bootstrap Input Group Accessibility
**Learning:** Bootstrap input groups often use `span.input-group-text` which lacks accessibility.
**Action:** Replace `span.input-group-text` with `label.input-group-text` and add `for` attributes to associate with inputs.
